server:
  port: 8080
  servlet:
    context-path: /
mybatis-plus:
  global-config:
    db-config:
      table-prefix: tbl_
    #  id-type: auto
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/HebeuOJ?serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: MyNewPass4!
    username: root
  servlet:
    multipart:
      max-file-size: 10MB



